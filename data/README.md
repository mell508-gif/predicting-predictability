# Data

Main pipeline outputs are grouped by step.

- `step1_*`: compound representation outputs
- `step2_*`: signature representation outputs
- `step3_multimodal/`: predictability and downstream analysis outputs
- `archive/`: earlier exploratory outputs

Other files at this level are source or matching tables used by the pipeline.
